/* Backward-compatible adapter for existing Azure Retirement Motion builders.
   The shared versioned model is the only implementation source. */
module.exports = require('../../../skills/retirement-intelligence-model/src');
